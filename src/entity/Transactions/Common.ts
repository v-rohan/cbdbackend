enum StatusOpts {
    pending = "pending",
    confirmed = "confirmed",
    declined = "declined",
    delayed = "delayed"
}

enum AcceptedStatusOpts {
    pending = "pending",
    confirmed = "confirmed",
    declined = "declined"
}

export {
    StatusOpts,
    AcceptedStatusOpts
}